install psycopg2 package
run "pip install psycopg2" command

modify data.txt file and add your database session info, database name, table name and the the data to be inserted into the columns.

data values should be preceded by "column_name:".
order of the columns does not matter.

finally, execute "run.bat" file